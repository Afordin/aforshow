---
import type { Participant } from '../../types/types'
const { participant } = Astro.props as { participant: Participant }
---

<picture
    class="rounded-full hover:gradient grayscale hover:grayscale-0 overflow-hidden p-2 bg-transparent max-w-[226px] transition-all"
>
    <img src={participant.img} alt={participant.name} />
</picture>
